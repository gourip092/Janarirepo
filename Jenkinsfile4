pipeline{
    agent {
        dockerfile true
    }
    stages {
        stage ('example') {
            steps{
                echo 'helloworld'
            }
        stage('Building image') {
            steps{
                script {
                    docker.build registry + ":$BUILD_NUMBER"
                }
        }
      }
    }
}
